<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by permission_handler to request the notification permission on
         Android 13+ (used by the push-notifications toggle). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="saobracaj"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- App Links: https://saobracaj.gleb.at/... opens the app instead
                 of the browser. android:autoVerify makes Android fetch
                 https://saobracaj.gleb.at/.well-known/assetlinks.json (served
                 by web_server/) and check this app's signing certificate
                 against it — until that file is live, the links keep opening
                 in the browser and nothing else breaks. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="saobracaj.gleb.at" android:pathPrefix="/invite"/>
                <data android:scheme="https" android:host="saobracaj.gleb.at" android:pathPrefix="/question"/>
                <data android:scheme="https" android:host="saobracaj.gleb.at" android:pathPrefix="/groups"/>
                <data android:scheme="https" android:host="saobracaj.gleb.at" android:pathPrefix="/konspekt"/>
                <data android:scheme="https" android:host="saobracaj.gleb.at" android:pathPrefix="/zakon"/>
            </intent-filter>

            <!-- The same addresses under the app's own scheme
                 (saobracaj://saobracaj.gleb.at/invite/ABC-DEF-GHI). No domain
                 verification is involved, so this route works on a sideloaded
                 build and wherever an in-app browser swallows App Links. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="saobracaj"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
